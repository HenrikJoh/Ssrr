const initialState = {
  pictures: [
    {
      id: 1,
      name: 'hehe'
    }
  ]
};

function gallery(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export default gallery;
